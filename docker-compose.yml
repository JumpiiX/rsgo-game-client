version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8080:8080"
    restart: unless-stopped
    container_name: rsgo-backend
    networks:
      - rsgo-network

networks:
  rsgo-network:
    driver: bridge